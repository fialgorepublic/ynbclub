<%= form_with(model: group_category, local: true) do |form| %>
  <div class="form-group">
    <%= form.label t('group_categories.name_label') %>
    <%= form.text_field :name, id: :category_name, class: 'form-control', required: true %>
  </div>

  <div class="actions">
    <%= form.submit action_name == 'edit' ? t(:update_label) : t('group_categories.new.create_category_label'), class: 'btn btn-success' %>
  </div>
<% end %>




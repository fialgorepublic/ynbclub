$('#join-group-link').text('Join Group');
$('#group-users-count').text(parseInt($('#group-users-count').text()) - 1);
$('#join-group-link').attr('href', '<%= join_user_group_path(current_user, params[:id]) %>');
window.location.reload()
<div class="col-md-5 col-lg-5 m-p-0 ">
  <div class="groups-cards">
    <div class="lib-panel">
      <div class="row box-shadow">
        <div class="">
          <%= image_tag group.logo , class: "lib-img-show" %>
        </div>
        <div class="">
          <div class="lib-row lib-header">
            <div class="grp-edit-delete-icon">
              <%= link_to edit_group_path(group), class: "bg-groups-circle" do %>
                <i class="fa fa-pencil inneer" aria-hidden="true"></i>
              <% end %>
              <%= link_to "javascript:void(0)", data: { url: group_path(group), name: group.name }, class: "bg-groups-circle delete-group-link" do %>
                <i class="fa fa-trash inneer" aria-hidden="true"></i>
              <% end %>
            </div>
          </div>
          <div class="lib-row lib-desc">
            <h2><%= group.name %></h2>
            <p><%= group.description %></p>
          </div>
          <div class="grp-card-icon">
            <i class="fa fa-users color-for-icon" width="100%"></i> 
            <p>0</p>
            <i class="fa fa-comment color-for-icon" width="100%"></i> 
            <p>0</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

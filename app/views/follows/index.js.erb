<% id = params[:type] == 'followers' ? '#follower-users' : '#following-users' %>
var divId = '<%= id %>';
$(divId).html('<%= j render partial: 'users/user', collection: @users, locals: { type: params[:type] } %>')

<%= form_with(model: point_type, local: true) do |form| %>
  <% if point_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point_type.errors.count, "error") %> prohibited this reward from being saved:</h2>

      <ul>
      <% point_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :point_type_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :point %>
    <%= form.text_field :point, id: :point_type_point, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit "Reward", class: 'btn btn-success update-text'  %>
  </div>
<% end %>

